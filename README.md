Clone this repo as `root` user:

```bash
git clone https://github.com/serhatcelik/arch-clean.git
```

Then run the commands below and follow the instructions:

```bash
cd arch-clean
chmod +x install.sh
./install.sh
```

**Tested under Arch Linux 2024.02.01**
