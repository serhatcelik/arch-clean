# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $alt Mod1
set $mod Mod4

# Font for window titles.
font pango:JetBrains Mono Regular 12

# Use pactl to adjust volume in PulseAudio.
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl get-sink-volume @DEFAULT_SINK@ | grep -q "15[0-9]%" || pactl set-sink-volume @DEFAULT_SINK@ +5%
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5%
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && sh "$HOME"/.local/bin/volume.sh mute
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle

# Use polybar backlight module to adjust brightness.
bindsym XF86MonBrightnessUp exec --no-startup-id polybar-msg action backlight hook 1
bindsym XF86MonBrightnessDown exec --no-startup-id polybar-msg action backlight hook 2

# Disable mouse focus, it is annoying.
focus_follows_mouse no

# Use Mouse+$mod to drag floating windows to their wanted position.
floating_modifier $mod

# Move tiling windows via drag and drop by left-clicking into the title bar.
tiling_drag modifier titlebar

# Kill focused window.
bindsym $mod+Shift+q kill

# Start a terminal.
bindsym $mod+Return exec --no-startup-id alacritty --config-file "$HOME"/.config/alacritty/alacritty.toml

# Start program launcher.
bindsym $mod+d exec --no-startup-id rofi -no-lazy-grab -show drun -config "$HOME"/.config/rofi/config.rasi -theme "$HOME"/.config/rofi/theme.rasi

# Change focus.
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move focused window.
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Split in horizontal orientation.
bindsym $mod+h split h

# Split in vertical orientation.
bindsym $mod+v split v

# Enter fullscreen mode for the focused container.
bindsym $mod+f fullscreen toggle

# Change container layout (stacked, tabbed, toggle split).
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Toggle tiling/floating.
bindsym $mod+Shift+space floating toggle

# Change focus between tiling/floating windows.
bindsym $mod+space focus mode_toggle

# Focus the parent container.
bindsym $mod+a focus parent

# Focus the child container.
bindsym $mod+c focus child

# Switch between two workspaces.
bindsym $alt+Tab workspace back_and_forth
bindsym $mod+Tab workspace back_and_forth

# Define names for default workspaces for which we configure key bindings later on.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# Switch to workspace.
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Move focused container to workspace.
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# Reload the configuration file.
bindsym $mod+Shift+c reload

# Restart i3 inplace (preserves your layout/session, can be used to upgrade i3).
bindsym $mod+Shift+r restart

# Exit i3 (logs you out of your X session).
bindsym $mod+Shift+e exec "i3-nagbar -t warning -f 'pango:JetBrains Mono Regular 12' -m 'Exit i3? This will end the X session.' -B 'Yes' 'i3-msg exit'"

# Resize window.
mode "RESIZE" {
    bindsym Left resize shrink width 5 px or 5 ppt
    bindsym Right resize grow width 5 px or 5 ppt
    bindsym Up resize grow height 5 px or 5 ppt
    bindsym Down resize shrink height 5 px or 5 ppt
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}
    bindsym $mod+r mode "RESIZE"

# Grab a logind suspend inhibit lock with xss-lock and use i3lock to lock the screen before suspend.
exec --no-startup-id xss-lock --transfer-sleep-lock -- sh "$HOME"/.local/bin/lock.sh
bindsym $mod+l exec --no-startup-id sh "$HOME"/.local/bin/lock.sh

# Start clipboard manager.
exec_always --no-startup-id copyq
bindsym $mod+q exec --no-startup-id copyq show

# Start compositing manager.
exec_always --no-startup-id picom --daemon --config "$HOME"/.config/picom/picom.conf

# Start notification daemon.
exec_always --no-startup-id dunst -config "$HOME"/.config/dunst/dunstrc

# Start screenshot app.
bindsym $mod+p exec --no-startup-id flameshot launcher

# Launch polybar.
exec_always --no-startup-id sh "$HOME"/.local/bin/polybar.sh

# Set wallpaper.
exec_always --no-startup-id sh "$HOME"/.fehbg
